import { Domain_HackathonResponses } from '@/api/@types'
import { HackathonRepository } from '@/repositories/HackathonRepository'
import { useEffect, useState } from 'react'

export const useUpdateModal = (roomId:string) => {
}
